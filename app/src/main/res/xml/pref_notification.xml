<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <SwitchPreference
        android:defaultValue="true"
        android:key="notifications_enable"
        android:title="@string/pref_notifications_enable" />

    <ListPreference
        android:defaultValue="Normal"
        android:dependency="notifications_enable"
        android:entries="@array/pref_notification_amount"
        android:entryValues="@array/pref_notification_amount_values"
        android:key="pref_notification_amount"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_notification_amount" />
</PreferenceScreen>